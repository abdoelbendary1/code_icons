import 'package:flutter/material.dart';

class AllTradeProveScreen extends StatelessWidget {
  const AllTradeProveScreen({super.key});

  static const routeName = "AllTradeProveScreen";

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AllTradeProveScreen'),
      ),
      body: const Center(
        child: Text('AllTradeProveScreen'),
      ),
    );
  }
}
