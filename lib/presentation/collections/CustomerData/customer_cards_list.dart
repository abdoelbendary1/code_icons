import 'package:flutter/material.dart';

class CustomersCardsScreen extends StatelessWidget {
  const CustomersCardsScreen({super.key});
  static const routeName = "CustomerslistScreen";
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
